# Pinned exactly, not floored. This package pins one exact Home Assistant
# version per release, so this line decides which HA the suite is tested
# against. A ">=" floor would let pip resolve backward, silently testing
# against an older HA than users run. Dependabot bumps it.
pytest-homeassistant-custom-component==0.13.365
mypy==2.3.1
homeassistant-stubs==2026.9.2
ruff==0.16.8
